# CRA-specific sshd hardening overlay.
# Merge into /etc/ssh/sshd_config after validating on the target Mac.

PasswordAuthentication no
PermitRootLogin no
PubkeyAuthentication yes
AuthorizedKeysFile .ssh/authorized_keys
AllowUsers steve.spivak
