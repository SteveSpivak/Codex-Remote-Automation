{
  "name": "cra-relay",
  "private": true,
  "version": "0.1.0",
  "description": "Transport-only WebSocket relay for CRA bridge sessions.",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  }
}
