---
description: Inspect the current CRA primary path and fallback status without mutating the repo.
---

From the repo root:

1. Run `codex app-server --help` and `codex exec --help`.
2. Run `node --check relay/server.js`.
3. Run `python3 -m cra.cli discover`.
4. Summarize the bridge-first architecture, the current fallback prototype status, and the main blockers to a live paired mobile client.
5. Treat Shortcuts, iMessage, Accessibility, and OCR findings as fallback-only unless the user explicitly asks to work on them.
