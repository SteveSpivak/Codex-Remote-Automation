---
description: Inspect the current CRA primary path and fallback status without mutating the repo.
---

From the repo root:

1. Run `codex app-server --help` and `codex exec --help`.
2. Run `python3 -m cra.cli discover`.
3. Summarize the App-Server-first architecture, the current fallback prototype status, and the main blockers to a live broker.
4. Treat Accessibility and OCR findings as fallback-only unless the user explicitly asks to work on them.
