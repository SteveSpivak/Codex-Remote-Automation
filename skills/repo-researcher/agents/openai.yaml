interface:
  display_name: "Repo Researcher"
  short_description: "Map unfamiliar repos and entrypoints"
  default_prompt: "Use $repo-researcher to map this repository and identify the next files to read."
